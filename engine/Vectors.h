#pragma once
#include "Vector2.h"

namespace Vectors {
    inline constexpr Vector2 LEFT = Vector2(-1,0);
    inline constexpr Vector2 RIGHT = Vector2(1,0);
    inline constexpr Vector2 UP = Vector2(0,1);
    inline constexpr Vector2 DOWN = Vector2(0,-1);
    inline constexpr Vector2 ZERO = Vector2{};
}